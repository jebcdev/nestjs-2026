info:
  name: createNewBrand
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/api/v1/brands/
  body:
    type: json
    data: |-
      {
        "name":"Tesla"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
