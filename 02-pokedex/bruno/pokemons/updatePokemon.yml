info:
  name: updatePokemon
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{url}}/pokemons/1"
  body:
    type: json
    data: |-
      {
        "name":"Ivysaur"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
