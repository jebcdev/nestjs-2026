info:
  name: findAllPokemonsPaginated
  type: http
  seq: 7

http:
  method: GET
  url: "{{url}}/pokemons?take=10&skip=10"
  params:
    - name: take
      value: "10"
      type: query
    - name: skip
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
